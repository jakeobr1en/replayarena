#include <cstdio>

// Placeholder entry point. The gateway proper starts with issue #1
// (bounded MPSC request queue); see SPEC.md and .github/ISSUE_DRAFTS/.
int main() {
  std::puts("replayarena gateway v0.1.0-dev: nothing to serve yet.");
  return 0;
}
